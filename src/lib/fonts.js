// Import necessary modules from the 'next/font/google' package
import { Figtree } from "next/font/google";

// Create a font object for the Inter font with the 'latin' subset
export const figTree = Figtree({ subsets: ["latin"] });
